[![Build Status](http://localhost:8080/buildStatus/icon?job=PiJenkins.git)](http://localhost:8080/job/PiJenkins.git/)

# pi-challenge
Use a Jenkins pipeline job to run a script that computes the value of pi.
